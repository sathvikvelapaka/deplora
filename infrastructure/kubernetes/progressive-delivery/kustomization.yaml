apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# rollout.yaml deleted (ADR-016): it wrapped the TRAINING image as a
# pretend model server - one canary mechanism now, KServe-native traffic
# with this AnalysisTemplate as the standalone brain.
resources:
  - analysis-template.yaml
