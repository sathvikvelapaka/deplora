# PodDisruptionBudgets for Critical Platform Services
#
# These PDBs ensure high availability during voluntary disruptions
# such as node drains, cluster upgrades, and maintenance.
# Using maxUnavailable (not minAvailable) so single-replica deployments
# can still be drained — minAvailable: 1 blocks drains entirely when
# only one replica exists.

---
# MLflow Tracking Server
# Maintains experiment tracking availability during updates
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: mlflow-pdb
  namespace: mlflow
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: mlflow

---
# Argo Workflows Controller
# Ensures workflow orchestration remains available
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: argo-workflow-controller-pdb
  namespace: argo
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: workflow-controller

---
# Argo Server (UI)
# Keeps workflow UI accessible during maintenance
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: argo-server-pdb
  namespace: argo
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app: argo-server

---
# KServe Controller
# Manages InferenceService deployments
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: kserve-controller-pdb
  namespace: kserve
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      control-plane: kserve-controller-manager

---
# Karpenter Controller
# Critical for node provisioning - must stay available
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: karpenter-pdb
  namespace: karpenter
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: karpenter

---
# Prometheus (Monitoring)
# Maintains observability during cluster operations
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: prometheus-pdb
  namespace: monitoring
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: prometheus

---
# Grafana (Dashboards)
# Keeps dashboards accessible for troubleshooting
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: grafana-pdb
  namespace: monitoring
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: grafana

---
# External Secrets Operator
# Ensures secret synchronization remains available
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: external-secrets-pdb
  namespace: external-secrets
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: external-secrets

---
# ArgoCD Application Controller
# Ensures GitOps reconciliation remains available
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: argocd-application-controller-pdb
  namespace: argocd
spec:
  maxUnavailable: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: argocd-application-controller
